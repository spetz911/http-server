Для удобства навигации по коду:

В этой же директории запускаете $ ctags -R *

Далее окрываем, например, main.cpp в Vim и ставим курсор на ThreadQueue, нажимаетм  Ctrl+] и переходим
на определение этого темплэйта. :pop - и вернулись на один уровень выше.

Для гугления - Vim ctags
